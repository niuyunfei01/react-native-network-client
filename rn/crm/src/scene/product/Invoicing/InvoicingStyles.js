import pxToDp from "../../../util/pxToDp";

export default {
  in_cell: {
    minHeight: pxToDp(100),
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: "row",
  },
  in_h_cell: {
    minHeight: pxToDp(180)
  }
}
