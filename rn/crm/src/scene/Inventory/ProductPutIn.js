import React from "react";
import {View} from "react-native";

export default class ProductPutIn extends React.Component {
  static navigationOptions = ({navigation}) => {
    return {
      headerTitle: '商品入库'
    }
  }
  
  render () {
    return (
      <View></View>
    );
  }
}