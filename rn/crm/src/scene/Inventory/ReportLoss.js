import BaseComponent from "../BaseComponent";
import {View} from "react-native";

export default class ReportLoss extends BaseComponent {
  render (): React.ReactNode {
    return (
      <View></View>
    )
  }
}